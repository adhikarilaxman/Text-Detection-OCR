Flask>=3.0.0
flask-cors>=4.0.0
openai>=1.0.0
python-dotenv>=1.0.0

# Note: heavy native libraries required for local OCR (easyocr/opencv/pytesseract)
# are intentionally omitted here to keep the Vercel serverless build small.
# For local development, use `backend/requirements-local.txt` which lists
# those heavier dependencies.
